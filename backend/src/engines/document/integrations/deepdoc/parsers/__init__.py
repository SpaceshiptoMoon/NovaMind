"""DeepDoc 解析器集合（docx / epub / excel / pdf / ppt / html / markdown / json / txt / figure）。

包级不再聚合导出——消费方直接 import 具体格式子模块（`parsers.pdf` /
`parsers.figure` / `parsers.upstream.markdown_parser` 等）。
"""
