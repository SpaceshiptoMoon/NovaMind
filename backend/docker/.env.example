# NovaMind 后端开发环境 Docker Compose 所需变量
# 用法：cp .env.example .env，然后将下面的占位符替换为你的真实值
# 注意：.env 文件已被 gitignore，请勿提交真实密码

# MySQL root 密码
MYSQL_ROOT_PASSWORD=change-me-mysql-password

# MinIO 管理员账号 / 密码
MINIO_ROOT_USER=change-me-minio-user
MINIO_ROOT_PASSWORD=change-me-minio-password
